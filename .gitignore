# ── Keystore (NEVER commit these) ─────────────────────────
android/keystore/
android/app/key.properties
*.jks
*.keystore

# ── Firebase (replace with your own) ──────────────────────
# Uncomment if you want to keep these private:
# android/app/google-services.json
# ios/Runner/GoogleService-Info.plist

# ── Flutter / Dart ─────────────────────────────────────────
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.lock

# ── IDE ────────────────────────────────────────────────────
.idea/
.vscode/
*.iml

# ── OS ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Python scripts (build tools only) ─────────────────────
*.py
